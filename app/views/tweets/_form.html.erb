<%= form_with(model: @tweet, local: true) do |form| %>
  <% if @tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="control">
      <%= form.text_area :tweet, class: "textarea", placeholder: "Compose a new tweet ...", rows: 5 %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit "create tweet", class: "button is-info" %>
  </div>

<% end %>
